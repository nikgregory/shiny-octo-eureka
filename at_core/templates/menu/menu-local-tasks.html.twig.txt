{#
/**
 * @file
 * Default theme implementation to display primary and secondary local tasks.
 *
 * Available variables:
 * - primary_tasks: HTML list items representing primary tasks.
 * - secondary_tasks: HTML list items representing primary tasks.
 *
 * Note: Each item for both sets of tasks can be individually themed in
 * menu-local-task.html.twig.
 *
 * @see template_preprocess_menu_local_tasks()
 *
 * @ingroup themeable
 */
#}
{% if primary or secondary %}
<nav class="local-tasks">

  {% if primary %}
    <h2 class="visually-hidden">{{ 'Primary local tasks'|t }}</h2>
    <ul class="local-tasks--menu local-tasks__primary">{{ primary }}</ul>
  {% endif %}

  {% if secondary %}
    <h2 class="visually-hidden">{{ 'Secondary local tasks'|t }}</h2>
    <ul class="local-tasks--menu local-tasks__secondary">{{ secondary }}</ul>
  {% endif %}

</nav>
{% endif %}